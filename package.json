{
  "name": "content-factory",
  "version": "1.0.0",
  "description": "Content Factory Workers",
  "scripts": {
    "dev:main": "wrangler dev -c wrangler.toml",
    "deploy:main": "wrangler deploy -c wrangler.toml",
    "dev:drive": "wrangler dev -c wrangler.drive.toml",
    "deploy:drive": "wrangler deploy -c wrangler.drive.toml",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
